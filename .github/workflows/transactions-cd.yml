name: 🚀 Deploy Transactions Service to Dokku

on:
  push:
    branches:
      - features/transactions
      # - main

jobs:
  deploy:
    runs-on: self-hosted
    environment: transactions  # 👈 environment name

    steps:
      - name: 🧭 Checkout code
        uses: actions/checkout@v4

      - name: 🔑 Start SSH agent and add key
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.DOKKU_SSH_PRIVATE_KEY }}

      - name: 🚀 Deploy to Dokku
        run: |
          ssh-keyscan -H "$DOKKU_HOST" >> ~/.ssh/known_hosts
          git remote add dokku dokku@$DOKKU_HOST:$DOKKU_APP || true
          git push dokku `git subtree split --prefix transactions HEAD`:refs/heads/main --force
        env:
          DOKKU_HOST: ${{ secrets.DOKKU_HOST }}
          DOKKU_APP: ${{ secrets.DOKKU_APP }}
